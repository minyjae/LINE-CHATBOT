name: line-chat-bot

services:
    api:
      build:
        context: .
        dockerfile: Dockerfile
      container_name: line-chat-bot-api
      restart: always
      env_file:
        - .env
      environment:
        # override host ให้ชี้ไปที่ container postgres แทน localhost
        DB_HOST: postgres
        DB_PORT: 5432
      ports:
        - "${APP_PORT}:8080"
      depends_on:
        postgres:
          condition: service_healthy
      networks:
        - line-chat-bot

    postgres:
      image: postgres:16-alpine
      container_name: line-chat-bot-db
      restart: always
      environment:
        POSTGRES_USER: ${DB_USER}
        POSTGRES_PASSWORD: ${DB_PASSWORD}
        POSTGRES_DB: ${DB_NAME}
        SECRET_KEY: ${SECRET_KEY}
      ports:
        - "${DB_PORT}:5432"
      volumes:
        - postgres_data:/var/lib/postgresql/data
      healthcheck:
        test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
        interval: 5s
        timeout: 5s
        retries: 5
      networks:
        - line-chat-bot

    pgadmin:
      image: dpage/pgadmin4:latest
      container_name: line-chat-bot-pgadmin
      restart: always
      environment:
        PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
        PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
      ports:
        - "${PGADMIN_PORT}:80"
      depends_on:
        - postgres
      volumes:
        - pgadmin_data:/var/lib/pgadmin
      networks:
        - line-chat-bot

volumes:
  postgres_data:
  pgadmin_data:

networks:
  line-chat-bot:
    name: line-chat-bot
    driver: bridge
